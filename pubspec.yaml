name: gasolina_ou_alcool
description: "Um aplicativo completo e bonito para calcular a melhor opção entre gasolina e álcool."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Ajuste conforme sua versão do Dart/Flutter

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # Nossas dependências adicionadas
  google_fonts: ^6.2.1
  intl: 0.20.2 # Usado para formatação de moeda

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Registro dos nossos assets (imagens)
  assets:
    - assets/images/